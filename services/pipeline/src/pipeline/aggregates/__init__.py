"""Aggregate generation (Sprint 6 / Phase 26).

Reads eligible ``fact.*`` rows and writes ``analytics.*`` aggregate rows under an
aggregate run. Task 26.1 lands the run lifecycle plus charge-only OUTCOME
aggregates; sentencing (26.2), judge-specific (27.x), validation (28.1), and
publish (28.2) arrive in later tasks.
"""
