{
  "name": "@pca/taxonomy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.11"
  },
  "main": "./generated/index.ts",
  "exports": {
    ".": "./generated/index.ts"
  },
  "scripts": {
    "validate": "tsx src/validate.ts",
    "generate": "tsx src/generate.ts",
    "lint": "eslint .",
    "typecheck": "tsx src/generate.ts && tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "tsx": "^4.23.0",
    "vitest": "^3.0.0"
  }
}
