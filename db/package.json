{
  "name": "@pca/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "migrate:latest": "tsx --env-file-if-exists=../.env src/migrate.ts latest",
    "migrate:up": "tsx --env-file-if-exists=../.env src/migrate.ts up",
    "migrate:down": "tsx --env-file-if-exists=../.env src/migrate.ts down",
    "migrate:status": "tsx --env-file-if-exists=../.env src/migrate.ts status",
    "seed": "pnpm --filter @pca/taxonomy run generate && tsx --env-file-if-exists=../.env seeds/run.ts",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pca/taxonomy": "workspace:*",
    "kysely": "^0.29.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/pg": "^8.20.0",
    "tsx": "^4.23.0",
    "vitest": "^3.0.0"
  }
}
